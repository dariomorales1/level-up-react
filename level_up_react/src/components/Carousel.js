import React from 'react';
import './Carousel.css';

const Carousel = () => {
    return (
        <div id="carrousel">
            {/* Contenido del carousel aquí */}
            Carousel Content
        </div>
    );
};

export default Carousel;